[pytest]
# Put the repo root on sys.path so `pytest` (and CI's `pytest -q`) can `import app`
# without an editable install. Without this, only `python -m pytest` works.
pythonpath = .
testpaths = tests
